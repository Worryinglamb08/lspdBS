<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>Register - lspdBS</title>
    {% include 'partials/head.html.twig' %}
</head>

<body class="bg-gradient-primary" style="background-image: url({{ asset('assets/img/CheerfulBlindBordercollie-size_restricted.gif') }});">
<div class="container">
    {% include 'partials/flash.html.twig' %}
    <div class="card shadow-lg o-hidden border-0 my-5">
        <div class="card-body p-0">
            <div class="row">
                <div class="col-lg-5 d-none d-lg-flex">
                    <div class="flex-grow-1 bg-register-image" style="background-image: url({{ asset('assets/img/badge.jpg') }});"></div>
                </div>
                <div class="col-lg-7">
                    <div class="p-5">
                        <div class="text-center">
                            <h4 class="text-dark mb-4">Demande de compte</h4>
                        </div>
                        {{ form_start(form) }}
                            <div class="form-group row">
                                <div class="col-sm-6 mb-3 mb-sm-0">
                                    {{ form_widget(form.nom) }}
                                    {{ form_errors(form.nom) }}
                                </div>
                                <div class="col-sm-6">
                                    {{ form_widget(form.prenom) }}
                                    {{ form_errors(form.prenom) }}
                                </div>
                            </div>
                            <div class="form-group">
                                {{ form_widget(form.mail) }}
                                {{ form_errors(form.mail) }}
                            </div>
                            <div class="form-group row">
                                <div class="col-sm-6 mb-3 mb-sm-0">
                                    {{ form_widget(form.pwd) }}
                                    {{ form_errors(form.pwd) }}
                                </div>
                                <div class="col-sm-6">
                                    {{ form_widget(form.confPwd) }}
                                    {{ form_errors(form.confPwd) }}
                                </div>
                            </div>
                        <button class="btn btn-primary btn-block text-white btn-user" type="submit">Envoyer la demande</button>
                            <hr>
                            <h4 class="text-dark mb-4">Votre demande sera ensuite validé par un cadre supérieur de la LSPD</h4>
                            <hr>
                        {{ form_end(form) }}
                        <div class="text-center"><a class="small" href="forgot-password.html">Mot de passe oublier ?</a></div>
                        <div class="text-center"><a class="small" href="login.html">Vous avez un compte? Connexion!</a></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% include 'partials/js.html.twig' %}
</body>

</html>
